services:
  - type: web
    name: weekly-report-formatter
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    branch: main
    autoDeploy: true
    envVars:
      - key: PORT
        value: 10000
